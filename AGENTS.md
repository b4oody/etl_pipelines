<!-- # Agent Instructions

- Before editing files, explain the intended changes and wait for explicit user approval.
- Do not call `apply_patch` or otherwise modify files until the user approves the proposed changes.
- After applying approved changes, list every file that was changed.
- When useful, show the relevant `git diff` or summarize the exact edits before asking for approval.
- Reading files, searching code, and proposing plans is allowed without approval. -->
